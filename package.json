{
  "name": "voice-registry-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for anchoring voiceprints",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network monad_testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0",
    "dotenv": "^16.3.1"
  }
}
